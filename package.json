{
  "name": "secure-visibility-api",
  "version": "1.0.0",
  "description": "Protect Electron app content from being captured or recorded.",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build && electron .",
    "build": "npm run build:native && npm run build:ts",
    "build:native": "node-gyp rebuild --arch=x64",
    "build:ts": "tsc"
  },
  "keywords": [
    "electron",
    "security",
    "privacy",
    "capture-protection"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^28.0.0",
    "node-gyp": "^10.3.1",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^7.1.0"
  }
}

